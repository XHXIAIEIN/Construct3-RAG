{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git commit'; then /c/Users/test/AppData/Local/Python/bin/python.exe -m pytest tests/ -q --tb=short 2>&1 | tail -5; fi"
          }
        ]
      }
    ]
  }
}
