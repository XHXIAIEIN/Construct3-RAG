# Construct 3 RAG Configuration
# Copy to .env and edit as needed

# Construct data. The runtime reads the committed data/ directory; set
# C3_SCHEMA_DIR only to point it at another schema export.
# C3_VERSION=r495.2
# C3_SCHEMA_DIR=D:/path/to/schemas
# RAG_SERVER_PORT=8765
