"""Everything specific to the optional Qdrant vector-retrieval stack.

Collection metadata, the dense/sparse vector adapters, the semantic retrieval
implementation, and the Qdrant publication adapter live here so the whole
optional path can be reasoned about, tested, or removed as one unit.
"""
