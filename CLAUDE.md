# Construct3-RAG 项目工作章程

@AGENTS.md

本文件定义产品方向、判断原则和完成标准。`AGENTS.md` 是仓库地图和操作 SOP，
各目录的 `CLAUDE.md` 说明该目录的结构和局部约束；两者不得与这里冲突。三者
不重复：改代码的步骤、数据字段和模块职责以那两处为准，这里只写别处没有的
内容。

## 1. 产品定位

Construct3-RAG 首先是面向 Construct 3 问答和工具集成的结构化参考数据集。
仓库内提交的数据能被 LLM、脚本和其他应用直接读取，不要求先启动数据库、
模型或 API 服务。搜索 API 是数据之上的可选能力：Direct Lookup 处理可证明
的精确查询，Qdrant、Embedding 和 Reranker 属于可选完整模式。

本项目不是通用聊天机器人，也不以堆叠常见 RAG 技术为目标。优先保障：

1. 准确查找插件、行为、ACE、特效和脚本接口。
2. 返回可核对、可引用的中英文官方数据。
3. 查找与插件、行为或主题相关的官方示例项目。
4. 在不安装外部服务的情况下直接消费仓库数据。
5. 可选搜索服务能区分精确查询与需要语义检索的问题。

## 2. 产品先于实现

不要因为代码、配置、测试或文档已经存在，就假定对应功能应该保留。历史实现
可能来自错误的问题定义，后续补丁、测试和注释也可能受到污染。测试只能证明
实现满足了测试写下的预期，不能证明预期本身正确；测试固化了错误产品行为时，
先修正预期，再改测试。

处理重要功能或重构时，先回答：

1. 它解决哪个真实用户任务？
2. 当前默认运行路径是否实际调用它？
3. 有什么真实查询、日志、用户反馈或基准数据支持它？
4. 更简单的实现或直接读取数据能否解决同一问题？
5. 收益是否覆盖代码、依赖、部署和维护成本？

结论是以下之一：保留、简化、重写、删除；证据不足时先调研，建立基线和实验，
不直接修改生产路径。

作方案时：先定义用户可观察的成功结果，再决定模块和算法；对复杂方案先建
更简单的基线；先检查问题是否来自数据质量、字段权重、路由或产品边界，再
考虑增加关键词、提示词或模型层；关键产品选择会产生明显不同结果时，把证据、
选项和取舍写进 `docs/decisions/`。

## 3. 能力边界

| 能力 | 定位 |
|---|---|
| `data/` 下已提交的 Schema、示例、脚本接口和语言包 | 核心，无需服务即可读取 |
| CDN 拉取、导出和更新工作流 | 核心数据维护能力 |
| Schema Direct Lookup、FastAPI 服务 | 可选，必须严格控制错误截获 |
| Qdrant 语义检索、多集合路由、Reranker | 可选完整模式；去留要用同集基准证明相对简单检索的收益 |

改变上表状态需要产品证据和决策记录。已删除的能力（QueryExpander、Lookup
Tier 2/3、Semantic Chain/HyDE）及其原因在 `docs/decisions/refactoring-audit.md`；
类似复杂能力只有在独立实验和基准证明有效后才可重新引入。

查询理解相关工作先读 `docs/decisions/query-understanding-refactor-requirements.md`，
其中"产品方向与历史假设复核"是关键词、扩展和提示词改动前的强制门禁；查询
扩展、同义词、LLM 校验和评估指标的具体规则以该文件为准。

## 4. 不变约束

- 默认路径离线、确定性、可解释。模块导入和普通查询不得隐式下载数据、加载
  大型模型或访问网络；刷新 CDN 数据是显式的维护操作。
- 外部服务和实验能力必须显式配置、可检测、可关闭并能安全回退。LLM 结果
  不能覆盖高置信度的确定性结果。
- 配置必须有真实运行调用者。删除功能时同时删除失效配置、依赖、测试和文档，
  并在决策记录里写明原因；不以"可能以后使用"为由保留未调用实现。
- 增加新词、提示词或模型前，必须有失败案例和可验证的预期结果。
- 公共 API 变更同步更新模型、文档和兼容测试；内部历史结构不承诺兼容。
- Construct 3 事实按 `AGENTS.md` 第 2 节查数据，不凭记忆作答。数据契约、
  模块职责和测试约定分别见 `data/CLAUDE.md`、`src/CLAUDE.md`、`tests/CLAUDE.md`。
- 代码使用类型提示和 `pathlib.Path`，捕获具体异常并在边界记录上下文。注释
  解释原因和限制，不复述代码。私密数据和机器本地配置不进仓库。
- README 先说数据如何直接使用，再说可选服务；中英文 README 描述同一行为，
  术语和路径一致。`docs/dev/architecture.md` 只描述真实运行架构。
- Playground 是调试界面，保持简洁并与现有样式一致，不增加装饰性组件。

## 5. 验证

默认单元测试在没有 Qdrant、GPU 和外部网络的环境中运行。`AGENTS.md` 第 5 节
的三条命令是底线；按修改范围补充：

- 数据布局：运行 `scripts/init.py` 的相关路径，检查 Schema 数量和结构。
- Direct Lookup：用真实提交数据执行代表性中英文查询。
- API：检查 `/health` 与 `/search`，区分 Schema 就绪和 Qdrant 状态。
- Qdrant 或 Reranker：只有服务真实运行时才能声称现场验证。
- 更新工作流：解析 YAML，确认使用导出器的标准目录。
- 查询质量：运行 `tests/CLAUDE.md` 列出的金标集，检查必须结果和禁止结果。

不在文档中硬编码测试总数。第三方弃用警告记录即可，不算项目失败。

## 6. 完成定义

任务只有同时满足以下条件才算完成：

1. 解决了用户可观察的问题，而不是只完成代码移动或测试适配。
2. 方案符合产品方向，并与简单基线比较过。
3. 真实默认运行路径已验证，不只是导入或构建成功；失败和回退行为可解释。
4. 第 5 节的检查通过，已扫描旧路径、旧集合名和失效配置的残余引用。
5. 没有留下死配置、孤立测试或失真文档；受行为变化影响的文档已更新。
6. 汇报区分已验证、未验证、保留风险和下一步决策；模拟测试不描述为现场验证。
