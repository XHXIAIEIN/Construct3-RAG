# Construct 3 RAG 评估报告

**模式**: `smart` | **题数**: 15 | **平均耗时**: 61.5s

## 总体评分

| 维度 | 得分 | 权重 |
|------|------|------|
| 关键词覆盖率 | 0.82 | 40% |
| 引用标注率 | 0.87 | 30% |
| 置信度质量 | 0.76 | 30% |
| **综合得分** | **0.82** | - |

等级分布: A=8 B=6 C=1 D=0

## 分类统计

| 分类 | 题数 | 综合 | 关键词 | 引用 | 置信度 |
|------|------|------|--------|------|--------|
| 工作流 | 3 | 0.81 | 0.72 | 1.00 | 0.73 |
| 插件 | 3 | 0.86 | 1.00 | 0.67 | 0.87 |
| 概念 | 3 | 0.87 | 0.78 | 1.00 | 0.87 |
| 系统 | 3 | 0.88 | 0.89 | 1.00 | 0.73 |
| 脚本 | 1 | 0.68 | 0.50 | 1.00 | 0.60 |
| 边界 | 2 | 0.66 | 0.83 | 0.50 | 0.60 |

## 逐题结果

| ID | 分类 | 综合 | 等级 | 关键词 | 引用 | 置信度 | 耗时 |
|----|------|------|------|--------|------|--------|------|
| B01 | 概念 | 1.00 | A | 1.00 | 1.00 | 1.00 | 56.3s |
| B02 | 概念 | 0.73 | B | 0.33 | 1.00 | 1.00 | 19.7s |
| B03 | 概念 | 0.88 | A | 1.00 | 1.00 | 0.60 | 49.7s |
| B04 | 插件 | 0.70 | B | 1.00 | 0.00 | 1.00 | 0.1s |
| B05 | 插件 | 0.88 | A | 1.00 | 1.00 | 0.60 | 127.6s |
| B06 | 插件 | 1.00 | A | 1.00 | 1.00 | 1.00 | 37.0s |
| B07 | 系统 | 0.75 | B | 0.67 | 1.00 | 0.60 | 67.6s |
| B08 | 系统 | 0.88 | A | 1.00 | 1.00 | 0.60 | 57.9s |
| B09 | 系统 | 1.00 | A | 1.00 | 1.00 | 1.00 | 31.7s |
| B10 | 工作流 | 1.00 | A | 1.00 | 1.00 | 1.00 | 24.6s |
| B11 | 工作流 | 0.68 | B | 0.50 | 1.00 | 0.60 | 55.7s |
| B12 | 工作流 | 0.75 | B | 0.67 | 1.00 | 0.60 | 321.4s |
| B13 | 脚本 | 0.68 | B | 0.50 | 1.00 | 0.60 | 45.2s |
| B14 | 边界 | 0.88 | A | 1.00 | 1.00 | 0.60 | 11.7s |
| B15 | 边界 | 0.45 | C | 0.67 | 0.00 | 0.60 | 16.8s |

## 详细回答

### B01 [A] Sprite 对象是什么？它的主要用途是什么？

**置信度**: high | **耗时**: 56.3s | **综合**: 1.00

**回答**:

> Sprite 对象是 Construct 3 中用于创建和管理图像和动画的基本对象类型。它的主要用途是显示图像、播放动画，并通过各种动作和行为来控制其显示效果和交互行为。
> 
> ### Sprite 对象的用途
> - 显示图像：Sprite 对象可以加载图像并显示在游戏场景中。
> - 动画播放：Sprite 对象支持序列帧动画，可以创建和播放多个动画。
> - 交互控制：通过行为和动作，可以控制 Sprite 对象的移动、旋转、碰撞等。
> 
> ### 主要功能
> - **图像和动画管理**：通过 `getAnimation`、`getAllAnimations`、`addAnimation`、`removeAnimation`、`addAnimationFrame` 和 `removeAnimationFrame` 等方法，可以管理 Sprite 对象的动画和帧。[来源: 4]
> - **生成其他对象**：通过 `Spawn` 动作，可以在 Sprite 对象上创建其他对象。新对象将继承此对象的角度。[来源: 1]
> 
> ### 使用场景
> - **判断是否存在**：使用条件（Conditions）来判断 Sp

✓ 命中关键词: Sprite, 图像, 动画

### B02 [B] 事件表（Event Sheet）是什么？

**置信度**: high | **耗时**: 19.7s | **综合**: 0.73

**回答**:

> 事件表（Event Sheet）是 Construct 3 中用于定义游戏逻辑的核心组件。它是一系列事件（Events）的集合，这些事件描述了游戏中的行为和交互规则。每个布局（Layout）都可以关联一个事件表，用于定义该布局的逻辑。事件表可以被多个布局共享，以避免重复编写相同的逻辑。[来源: 3]
> 
> 事件表可以通过两种方式在多个布局之间共享：
> 1. 设置多个布局的 **Event sheet** 属性为同一个事件表。
> 2. 创建一个包含通用事件的独立事件表，然后通过 **事件表包含（Event sheet includes）** 功能将其包含到其他事件表中。[来源: 5]
> 
> 事件表可以通过右键点击项目栏中的事件表文件夹（如根级别的 *Event sheets* 文件夹）并选择 `Add event sheet` 来添加。事件表可以被重命名或删除。[来源: 7]
> 
> 事件表中的事件可以通过右键点击事件表并选择 `Include event sheet` 来包含另一个事件表，或者使用快捷键 <kbd>N</kbd>。Construct 会自动防止循环包含。[来源: 4]
> 
> 事件表的名称可以

✓ 命中关键词: 事件
✗ 缺失关键词: 条件, 动作

### B03 [A] 实例变量和全局变量的区别是什么？

**置信度**: medium | **耗时**: 49.7s | **综合**: 0.88

**回答**:

> 实例变量和全局变量的区别主要体现在它们的**作用域**和**访问范围**上：
> 
> - **实例变量**（局部变量）是变量放置在嵌套事件或组内，它们的**作用域仅限于其所在的事件层级**。这意味着，只有在其作用域内的事件可以访问这些变量，而位于更高层级（更少缩进）的事件无法访问它们。[来源: 6]
> 
> - **全局变量**是放置在事件表的顶层，它们的**作用域覆盖整个项目**。这意味着，任何事件、布局或事件表都可以访问这些变量。[来源: 7]
> 
> ### 使用场景建议：
> 
> - **判断是否存在**：如果需要判断一个变量的值是否符合特定条件（如是否为真、是否等于某个值），可以使用系统条件中的“比较变量”（Compare variable）来实现。[来源: 4,5]
> 
> - **获取位置**：如果需要获取某个变量的值（例如在事件表中使用该值进行计算或显示），可以通过表达式（Expressions）来获取该变量的值。[来源: 5]
> 
> - **重置变量**：如果需要将所有变量重置为初始值，可以使用系统动作中的“重置全局变量”（Reset global variables）来实现。[来源: 3]
> 
> 文档未

✓ 命中关键词: 实例, 全局

### B04 [B] Platform 行为有哪些主要参数？

**置信度**: high | **耗时**: 0.1s | **综合**: 0.70

**回答**:

> **平台 (Platform)** 行为的属性 (Properties)，共 10 项：
> 
> | # | 名称 | 英文名 | 说明 |
> |---|------|--------|------|
> | 1 | 最大移动速度 | Max speed | 角色在移动时的最大速度，以像素/秒为单位。 |
> | 2 | 加速度 | Acceleration | 从静止状态到正常移动速度的加速度，以像素/秒为单位。 |
> | 3 | 减速度 | Deceleration | 从正常移动至完全停止的减速度，以像素/秒为单位。 |
> | 4 | 跳跃高度 | Jump strength | 跳跃的初始速度，以像素每秒为单位。 |
> | 5 | 重力 | Gravity | 重力加速度，以像素/秒为单位。 |
> | 6 | 最大下落速度 | Max fall speed | 下落时的最大速度，以像素/秒为单位。 |
> | 7 | 二段跳 | Double-jump | 在落地前，允许在空中进行一次额外的跳跃。 |
> | 8 | 跳跃保持 | Jump sustain | 在跳跃状态，根据长按跳跃键的时长，让角色在空中可

✓ 命中关键词: Platform, 跳跃, 速度

### B05 [A] Tween 行为怎么用？如何让对象移动到指定位置？

**置信度**: medium | **耗时**: 127.6s | **综合**: 0.88

**回答**:

> 要使用 Tween 行为来让对象移动到指定位置，可以使用以下方法：
> 
> ### 1. 使用 `Set end value` 动作设置目标值
> 你可以使用 `Set end value` 动作来设置 Tween 的目标值，从而让对象移动到指定位置。这个动作可以设置 X 坐标、Y 坐标、宽度等属性的目标值。
> 
> - **动作名称**：`Set end value`  
> - **脚本名称**：`SetOnePropertyTweensEndValue`  
> - **参数**：  
>   - `标签`（string）：用于标识 Tween 的标签  
>   - `属性`（combo）：可选值包括 `X坐标`, `Y坐标`, `宽度`  
>   - `目标值`（number）：要设置的目标值  
> 
> 示例：  
> - 设置对象的 X 坐标为目标值 100：  
>   - `标签`：`moveTo`  
>   - `属性`：`X坐标`  
>   - `目标值`：`100`  
> 
> [来源: 2]
> 
> ### 2. 使用 `Move to position` 动作
> 如果你希望对象立即移动到场景中的指定位置，可以使用 `Mov

✓ 命中关键词: Tween

### B06 [A] Keyboard 插件如何检测按键？on key pressed 和 is key down 的区别？

**置信度**: high | **耗时**: 37.0s | **综合**: 1.00

**回答**:

> Keyboard 插件检测按键主要通过两种方式：**条件 (Conditions)** 和 **表达式 (Expressions)**。它们分别适用于不同的使用场景，不是先后步骤，而是不同场景下的替代方案。
> 
> ### 1. 判断是否存在（使用条件）
> 用于在事件表中判断某个按键是否被按住，适用于逻辑判断场景。
> 
> - **Key is down**  
>   检测当前是否按住键盘按键。  
>   使用场景：判断某个特定按键是否被按住。  
>   [来源: 1]
> 
> - **Key code is down**  
>   检测按键码对应的按键是否被按住。  
>   使用场景：通过按键码（数字）判断按键状态。  
>   [来源: 2]
> 
> - **Left/right key is down**  
>   检测左/右功能键（如左 Shift、右 Ctrl）是否被按住。  
>   使用场景：区分左右功能键的状态。  
>   [来源: 5]
> 
> - **On key pressed**  
>   触发当特定按键被按下时。  
>   使用场景：当某个按键被按下时执行操作。  
>   [来源: 11]
> 
> - **On key co

✓ 命中关键词: Keyboard, 按键

### B07 [B] System 对象的 For each 条件如何使用？

**置信度**: medium | **耗时**: 67.6s | **综合**: 0.75

**回答**:

> System 对象的 `For each` 条件用于在事件表中对某个对象的每个实例重复执行一组动作。以下是其使用方式的说明：
> 
> ### 1. 添加 `For each` 条件
> 要使用 `For each` 条件，首先需要在事件表中添加一个条件。可以通过以下方式添加：
> 
> - 点击事件表中的“添加条件”按钮 [来源: 1]。
> - 或者使用“添加其他条件”功能 [来源: 4]。
> 
> ### 2. 选择对象
> 添加条件后，需要选择要遍历的对象。此时，系统会提示你选择一个对象。选择对象后，`For each` 条件将对这个对象的每个实例重复执行后续的动作。
> 
> - 选择对象的操作可以通过“选择要编辑条件的对象”进行 [来源: 5]。
> 
> ### 3. `For each` 条件的参数
> `For each` 条件的参数是对象（object），表示要遍历的对象实例。
> 
> - 该参数在 `construct3-schema` 中定义，用于指定遍历的对象 [来源: 3]。
> 
> ### 4. `For each` 条件的排序版本
> Construct 3 还提供了一个排序版本的 `For each` 条件，称为 `Fo

✓ 命中关键词: For each, 每个
✗ 缺失关键词: 循环

### B08 [A] 如何在 Construct 3 中实现计时器？

**置信度**: medium | **耗时**: 57.9s | **综合**: 0.88

**回答**:

> 在 Construct 3 中实现计时器，可以通过内置的计时器功能或通过 JavaScript 实现。以下是基于参考资料的说明：
> 
> ### 1. 使用内置计时器功能
> - **创建计时器**：在 Construct 3 中，可以使用“计时器”对象来创建和管理计时器。通过设置计时器名称（Timer name），可以标识不同的计时器。[来源: 1]
> - **设置计时器标签**：计时器的标签用于标识特定的计时器，可以在事件表中通过“计时器的标签”来指定。[来源: 4]
> - **检测暂停的计时器**：如果需要检测一个暂停的计时器，可以使用“要检测暂停的计时器的标签”来指定该计时器的名称。[来源: 5]
> 
> ### 2. 使用 JavaScript / TypeScript 实现
> - **在事件表中使用「脚本」动作内嵌 JS 代码**：可以通过“脚本”动作来编写 JavaScript 代码，实现自定义的计时器逻辑。[来源: 通用经验]
> - **在项目脚本文件中编写 JS/TS 模块**：也可以在项目脚本文件中编写 JavaScript 或 TypeScript 模块，并通过 runtime API 

✓ 命中关键词: 计时, Timer

### B09 [A] Wait for signal 和 Wait X seconds 有什么区别？

**置信度**: high | **耗时**: 31.7s | **综合**: 1.00

**回答**:

> **Wait for signal** 和 **Wait X seconds** 是 Construct 3 中两种不同的等待机制，它们的用途和行为有明显区别：
> 
> ### 1. **Wait for signal**（等待信号）  
> - 该动作会暂停后续动作的执行，直到某个带有相同标签的 **"发送信号"** 动作被触发。
> - 这是一个事件驱动的等待方式，适用于需要等待外部事件触发的场景。
> - 使用场景：当你希望在某个条件满足后才继续执行后续动作时，比如等待玩家按下按钮、敌人攻击完成等。
> - 该功能来源于系统插件的动作 **Wait for signal**，其脚本名称为 `WaitForSignal`，参数为 **标签 (string)**。  
> [来源: 6]
> 
> ### 2. **Wait X seconds**（等待时间）  
> - 该动作会暂停后续动作的执行，等待指定的秒数后继续执行。
> - 这是一个基于时间的等待方式，适用于需要等待固定时间的场景。
> - 使用场景：当你希望在一段时间后才继续执行后续动作时，比如等待动画播放完毕、等待玩家输入等。
> - 该功能来源于系统插件的动作 **

✓ 命中关键词: Wait

### B10 [A] 如何让玩家按空格键跳跃？用 Platform 行为的 Simulate control 动作实现

**置信度**: high | **耗时**: 24.6s | **综合**: 1.00

**回答**:

> 要让玩家按空格键跳跃，并使用 Platform 行为的 `Simulate control` 动作实现，可以按照以下方式操作：
> 
> ### 使用场景：判断是否存在按键按下（条件）
> 如果需要判断空格键是否被按下，可以使用 `Keyboard.key-is-down(key=32)` 条件。  
> [来源: 8]
> 
> ### 使用场景：模拟按键按下（动作）
> 如果需要模拟空格键被按下，以触发跳跃，可以使用 `Player.simulate-control(control=jump)` 动作。  
> [来源: 2, 6, 7, 8]
> 
> ### 使用场景：事件表中实现
> 在事件表中，可以设置如下事件：
> 
> - **条件**: `Keyboard.key-is-down(key=32)`  
> - **动作**: `Player.simulate-control(control=jump)`  
> 
> 这将实现当玩家按下空格键时，模拟按下跳跃键，从而触发跳跃行为。  
> [来源: 2, 6, 7, 8]
> 
> ### 使用场景：游戏手柄控制
> 如果需要使用游戏手柄控制跳跃，可以使用 `Player.simulate-con

✓ 命中关键词: Platform, simulate, 跳跃, 空格

### B11 [B] 如何实现碰撞检测？Sprite 和 Sprite 碰撞时触发事件

**置信度**: medium | **耗时**: 55.7s | **综合**: 0.68

**回答**:

> 要实现 Sprite 与 Sprite 碰撞时触发事件，可以通过以下方式：
> 
> ### 1. 启用碰撞功能
> 在事件表中，使用「精灵」插件的「**启用碰撞**」动作来启用对象的碰撞功能，使它能够触发碰撞事件。
> 
> - **动作**: `Set collisions enabled` [来源: 2]
> 
> ### 2. 检测碰撞是否已启用
> 如果需要判断碰撞功能是否已启用，可以使用「精灵」插件的「**已启用碰撞**」条件来检测。
> 
> - **条件**: `Collisions enabled` [来源: 4]
> 
> ### 3. 设置碰撞过滤（可选）
> 如果需要控制精灵与其他具有实体行为的对象之间的碰撞，可以使用「**设置实体碰撞过滤**」动作，通过标签来包含或排除碰撞对象。
> 
> - **动作**: `Set solid collision filter` [来源: 5]
> 
> ### 4. 碰撞事件触发
> 当两个 Sprite 之间发生碰撞时，Construct 3 会自动触发碰撞事件。这通常是在「碰撞」事件表中设置的。
> 
> - 碰撞事件可以通过事件表的「碰撞」条件来检测，例如「**碰撞**」或「**碰撞开始**」

✓ 命中关键词: 碰撞
✗ 缺失关键词: Overlapping

### B12 [B] 如何用事件表实现分数系统？包括变量定义和 UI 更新

**置信度**: medium | **耗时**: 321.4s | **综合**: 0.75

**回答**:

> 在 Construct 3 中实现分数系统，可以通过事件表来定义变量和更新 UI。以下是具体步骤和推荐用法：
> 
> ### 1. 定义分数变量
> 在事件表中，使用「变量」功能定义一个变量来存储分数。例如，定义一个名为 `score` 的变量，初始值为 0。
> 
> - **定义变量**：在事件表中使用「变量」动作，选择「定义变量」，输入变量名 `score`，并设置初始值为 0。
>   - 这是通过 Construct 3 的内置变量系统实现的，无需额外插件或表达式 [来源: 4]。
> 
> ### 2. 更新分数变量
> 在事件表中，使用「变量」动作来更新分数变量。例如，当玩家得分时，增加 `score` 的值。
> 
> - **增加分数**：在事件表中使用「变量」动作，选择「增加变量」，输入变量名 `score`，并设置增加的值（如 10）。
>   - 这是通过 Construct 3 的内置变量系统实现的，无需额外插件或表达式 [来源: 4]。
> 
> ### 3. 更新 UI 显示分数
> 在事件表中，使用「文本」对象来显示分数。当分数变量发生变化时，更新文本对象的文本内容。
> 
> - **更新文本**：在事件表中，使用「

✓ 命中关键词: 变量, 分数
✗ 缺失关键词: Text

### B13 [B] 在 Construct 3 脚本中如何获取一个对象的实例？

**置信度**: medium | **耗时**: 45.2s | **综合**: 0.68

**回答**:

> 在 Construct 3 脚本中获取一个对象的实例，通常需要通过 `IObjectClass` 接口的方法，例如 `getFirstInstance()`。以下是相关事实性信息：
> 
> - 通过 `runtime.objects.[ObjectName].getFirstInstance()` 可以获取对象类型的第一个实例。[来源: 2]
> - 实例通常通过 `IObjectClass` 方法访问，例如 `getFirstInstance()`。[来源: 6]
> - 获取实例所属的对象类型的名称，可以通过 `instance.objectType.name` 实现。[来源: 3]
> 
> ### 使用场景说明
> 
> #### 1. 获取对象的实例
> - 使用 `runtime.objects.[ObjectName].getFirstInstance()` 可以获取对象类型的第一个实例。[来源: 2]
> - 如果需要获取所有匹配条件的实例数量，可以使用 `runtime.objects.[ObjectName].getInstances().length`。[来源: 5]
> 
> #### 2. 获取实例所属的

✓ 命中关键词: runtime
✗ 缺失关键词: script

### B14 [A] Construct 3 支持 WebGPU 渲染吗？有什么限制？

**置信度**: medium | **耗时**: 11.7s | **综合**: 0.88

**回答**:

> 文档未找到相关信息。[来源: 1,2,3,4,5,6,7]
> 
> 
> ⚠️ **置信度提示**: 以上回答的置信度较低，可能原因：
> - 检索到的相关文档较少
> - 问题涉及多个复杂概念
> - 部分信息可能基于通用经验而非官方文档
> 
> 建议：核实关键信息，或提供更多上下文以获得更准确的回答。
> 

### B15 [C] Construct 3 r999 版本有哪些新功能？

**置信度**: medium | **耗时**: 16.8s | **综合**: 0.45

**回答**:

> 文档未找到相关信息。
> 
> 
> ⚠️ **置信度提示**: 以上回答的置信度较低，可能原因：
> - 检索到的相关文档较少
> - 问题涉及多个复杂概念
> - 部分信息可能基于通用经验而非官方文档
> 
> 建议：核实关键信息，或提供更多上下文以获得更准确的回答。
> 

✓ 命中关键词: 文档, 未找到
✗ 缺失关键词: 未提及
